/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  basePath: "/portfolio",
  // compiler: {
  //   styledComponents: true,
  // },
};

module.exports = nextConfig;
